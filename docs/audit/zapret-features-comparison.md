# Сравнение фич zapret-discord-youtube и Isolate

## Обзор

Анализ утилит и сервисных скриптов из `thirdparty/zapret-discord-youtube/` для выявления функционала, который отсутствует в Isolate.

---

## 1. Тестирование стратегий (`utils/test zapret.ps1`)

### Функционал zapret-discord-youtube

#### 1.1 Стандартные тесты (HTTP/Ping)
- **Параллельное тестирование** через runspace pool (до 8 параллельных проверок)
- **Три типа HTTP-тестов** для каждого URL:
  - HTTP/1.1
  - TLS 1.2
  - TLS 1.3
- **Ping-тесты** для DNS-серверов (1.1.1.1, 8.8.8.8, 9.9.9.9)
- **Конфигурируемые цели** через `targets.txt`
- **Автоматический выбор лучшей стратегии** по количеству успешных тестов

#### 1.2 DPI-чекеры (TCP 16-20 freeze detection)
- **Специализированные тесты** для обнаружения DPI-блокировки по паттерну 16-20KB
- **23 тестовых endpoint'а** от разных провайдеров:
  - Cloudflare, DigitalOcean, Hetzner, OVH, Oracle, AWS, Google Cloud, Fastly, Akamai, CDN77, Contabo, Scaleway, Constant
- **Детекция паттерна блокировки** (LIKELY_BLOCKED при 14-22KB и ошибке)
- **Параллельное выполнение** с настраиваемым таймаутом

#### 1.3 Управление тестами
- **Выбор режима**: все конфиги или выборочно
- **Сохранение результатов** в файл с timestamp
- **Аналитика**: подсчёт OK/ERROR/UNSUP для каждого конфига
- **Автоматическое восстановление** ipset после тестов
- **Graceful shutdown** при прерывании (Ctrl+C)

### Статус в Isolate

| Функция | Статус | Комментарий |
|---------|--------|-------------|
| HTTP/TLS тестирование | ❌ | Нет отдельного тестирования HTTP/TLS версий |
| Ping-тесты | ❌ | Нет ping-тестов для DNS |
| DPI 16-20KB детекция | ❌ | Нет специализированных DPI-чекеров |
| Параллельное тестирование | ✅ | Есть в strategy_engine |
| Конфигурируемые цели | ⚠️ | Частично через services |
| Автовыбор лучшей стратегии | ⚠️ | Есть scoring, но без автоматического выбора |
| Сохранение результатов | ❌ | Нет экспорта результатов тестов |

---

## 2. Автообновление (`utils/check_updates.enabled`, `service.bat`)

### Функционал zapret-discord-youtube

- **Проверка версии** через GitHub raw URL (`/.service/version.txt`)
- **Сравнение версий** (LOCAL_VERSION vs GITHUB_VERSION)
- **Автоматическое скачивание** новой версии (rar-архив)
- **Переключатель автопроверки** (enabled/disabled через файл-флаг)
- **Проверка при запуске** (если enabled)

### Статус в Isolate

| Функция | Статус | Комментарий |
|---------|--------|-------------|
| Проверка версии через GitHub | ✅ | `update_checker.rs` - GitHub Releases API |
| Сравнение версий | ✅ | Семантическое сравнение (major.minor.patch) |
| Автоматическое скачивание | ❌ | Только уведомление с ссылкой |
| Переключатель автопроверки | ❌ | Нет настройки вкл/выкл |
| Проверка при запуске | ⚠️ | Нужно вызывать вручную |

---

## 3. Установка как Windows-сервис (`service.bat`)

### Функционал zapret-discord-youtube

#### 3.1 Управление сервисом
- **Install Service**: парсинг bat-файла → создание Windows-сервиса через `sc create`
- **Remove Services**: остановка и удаление сервиса zapret + WinDivert
- **Check Status**: проверка состояния сервисов и процессов
- **Автозапуск**: `start= auto` при создании сервиса

#### 3.2 Диагностика (`Run Diagnostics`)
- **Base Filtering Engine** — проверка работы BFE
- **Proxy check** — обнаружение системного прокси
- **TCP timestamps** — проверка и автовключение
- **Конфликтующее ПО**:
  - Adguard
  - Killer services
  - Intel Connectivity Network Service
  - Check Point
  - SmartByte
  - VPN-сервисы
  - GoodbyeDPI и другие bypass-сервисы
- **WinDivert конфликты** — автоматическое удаление
- **Очистка кэша Discord**

#### 3.3 Дополнительные функции
- **Switch Game Filter** — переключение портов (12 vs 1024-65535)
- **Switch ipset** — режимы none/loaded/any
- **Update ipset list** — загрузка актуального списка IP
- **Update hosts file** — для Discord voice

### Статус в Isolate

| Функция | Статус | Комментарий |
|---------|--------|-------------|
| Установка как Windows-сервис | ❌ | Нет функционала |
| Удаление сервиса | ❌ | Нет функционала |
| Проверка статуса сервиса | ❌ | Нет функционала |
| Автозапуск при старте Windows | ❌ | Нет функционала |
| Диагностика BFE | ❌ | Нет проверки |
| Проверка системного прокси | ❌ | Нет проверки |
| TCP timestamps | ❌ | Нет проверки/включения |
| Детекция конфликтующего ПО | ❌ | Нет проверки |
| Очистка кэша Discord | ❌ | Нет функционала |
| Game Filter (порты) | ⚠️ | Есть в конфигах стратегий |
| Управление ipset | ⚠️ | Есть hostlists, но без режимов |
| Обновление hosts | ❌ | Нет функционала |

---

## 4. Конфигурация целей тестирования (`utils/targets.txt`)

### Функционал zapret-discord-youtube

```
# Формат:
KeyName = "https://host..."   -> HTTP/TLS + ping
KeyName = "PING:1.2.3.4"      -> Только ping

### Discord
DiscordMain           = "https://discord.com"
DiscordGateway        = "https://gateway.discord.gg"
...

### YouTube
YouTubeWeb            = "https://www.youtube.com"
...

### Public DNS (PING-only)
CloudflareDNS1111     = "PING:1.1.1.1"
GoogleDNS8888         = "PING:8.8.8.8"
```

### Статус в Isolate

| Функция | Статус | Комментарий |
|---------|--------|-------------|
| Конфигурируемые цели | ⚠️ | Через services, но другой формат |
| HTTP/TLS тесты | ❌ | Нет раздельного тестирования |
| Ping-only цели | ❌ | Нет поддержки |
| Группировка по сервисам | ✅ | Есть в services |

---

## 5. Версионирование (`.service/version.txt`)

### Функционал zapret-discord-youtube

- Простой текстовый файл с версией: `1.9.2`
- Используется для проверки обновлений

### Статус в Isolate

| Функция | Статус | Комментарий |
|---------|--------|-------------|
| Версионирование | ✅ | Через Cargo.toml + tauri.conf.json |

---

## 6. Документация (README.md)

### Ключевые разделы zapret-discord-youtube

1. **Использование** — пошаговая инструкция
2. **Описания файлов** — что делает каждый bat
3. **FAQ** — распространённые проблемы и решения
4. **Добавление ресурсов** — как расширить списки
5. **Лицензирование** — MIT

### Статус в Isolate

| Функция | Статус | Комментарий |
|---------|--------|-------------|
| Пользовательская документация | ⚠️ | Есть docs/, но техническая |
| FAQ | ❌ | Нет раздела FAQ |
| Инструкция по установке | ⚠️ | Частично в README |

---

## Сводная таблица отсутствующих фич

| # | Фича | Приоритет | Сложность | Описание |
|---|------|-----------|-----------|----------|
| 1 | **Windows Service** | 🔴 Высокий | Высокая | Установка/удаление как системный сервис с автозапуском |
| 2 | **Диагностика конфликтов** | 🔴 Высокий | Средняя | Проверка BFE, TCP timestamps, конфликтующего ПО |
| 3 | **DPI 16-20KB детекция** | 🟡 Средний | Средняя | Специализированные тесты для обнаружения паттерна блокировки |
| 4 | **HTTP/TLS раздельное тестирование** | 🟡 Средний | Низкая | Тесты HTTP/1.1, TLS 1.2, TLS 1.3 отдельно |
| 5 | **Экспорт результатов тестов** | 🟡 Средний | Низкая | Сохранение результатов в файл |
| 6 | **Автовыбор лучшей стратегии** | 🟡 Средний | Средняя | Автоматический выбор по результатам тестов |
| 7 | **Ping-тесты DNS** | 🟢 Низкий | Низкая | Проверка доступности DNS-серверов |
| 8 | **Очистка кэша Discord** | 🟢 Низкий | Низкая | Утилита для очистки кэша |
| 9 | **Обновление hosts** | 🟢 Низкий | Низкая | Для Discord voice |
| 10 | **Переключатель автопроверки обновлений** | 🟢 Низкий | Низкая | Настройка вкл/выкл |

---

## Рекомендации по приоритету реализации

### Фаза 1: Критический функционал (v1.x)

1. **Диагностика конфликтов** — пользователи часто сталкиваются с проблемами из-за конфликтующего ПО
   - Проверка Base Filtering Engine
   - Проверка TCP timestamps
   - Детекция Adguard, Killer, SmartByte, VPN
   - Проверка WinDivert конфликтов

2. **Windows Service** — ключевая фича для постоянной работы
   - Установка как сервис
   - Удаление сервиса
   - Проверка статуса
   - Автозапуск при старте Windows

### Фаза 2: Улучшение тестирования (v1.x+)

3. **DPI 16-20KB детекция** — помогает понять тип блокировки
4. **HTTP/TLS раздельное тестирование** — более точная диагностика
5. **Экспорт результатов** — для отладки и отчётов
6. **Автовыбор стратегии** — улучшение UX

### Фаза 3: Утилиты (v2.x)

7. **Ping-тесты DNS**
8. **Очистка кэша Discord**
9. **Обновление hosts**
10. **Настройки автообновления**

---

## Технические заметки

### Windows Service Implementation

```rust
// Возможная реализация через windows-service crate
// или через sc.exe команды

// Создание сервиса:
// sc create zapret binPath= "path\to\winws.exe args" DisplayName= "Zapret" start= auto

// Удаление:
// net stop zapret
// sc delete zapret
```

### Диагностика конфликтов

```rust
// Проверки через PowerShell/WMI:
// - sc query BFE | findstr RUNNING
// - netsh interface tcp show global | findstr timestamps
// - tasklist /FI "IMAGENAME eq AdguardSvc.exe"
// - sc query | findstr /I "Killer"
```

### DPI 16-20KB Detection

```rust
// Логика:
// 1. Запрос с Range: 0-262143 (256KB)
// 2. Если получено 14-22KB и ошибка → LIKELY_BLOCKED
// 3. Тестировать на разных провайдерах (Cloudflare, Hetzner, AWS...)
```

---

## Заключение

Основные пробелы Isolate по сравнению с zapret-discord-youtube:

1. **Отсутствие Windows Service** — критично для постоянной работы
2. **Нет диагностики конфликтов** — пользователи не понимают почему не работает
3. **Нет специализированных DPI-тестов** — сложнее понять тип блокировки

Рекомендуется начать с диагностики конфликтов (быстрее реализовать, большой impact на UX), затем Windows Service.
