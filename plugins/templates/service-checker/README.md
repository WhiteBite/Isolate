# Service Checker Plugin Template

Шаблон для создания плагина проверки доступности сервиса (Level 1).

## Быстрый старт

1. Скопируйте эту директорию:
   ```bash
   cp -r plugins/templates/service-checker plugins/my-service-checker
   ```

2. Отредактируйте `plugin.json`:
   - Измените `id` на уникальный идентификатор
   - Заполните информацию о сервисе
   - Добавьте эндпоинты для проверки

3. Перезапустите Isolate — плагин загрузится автоматически

## Структура plugin.json

### Обязательные поля

| Поле | Описание |
|------|----------|
| `id` | Уникальный идентификатор (kebab-case) |
| `name` | Отображаемое имя |
| `version` | Версия в формате semver |
| `type` | Должен быть `"service-checker"` |
| `service` | Конфигурация сервиса |

### Конфигурация сервиса

```json
{
  "service": {
    "id": "my-service",
    "name": "My Service",
    "icon": "🌐",
    "category": "social",
    "description": "Описание сервиса",
    "endpoints": [...]
  }
}
```

### Категории сервисов

- `social` — Социальные сети (Instagram, Twitter)
- `media` — Медиа и стриминг (YouTube, Spotify)
- `gaming` — Игровые платформы (Steam, Discord)
- `messaging` — Мессенджеры (Telegram, WhatsApp)
- `other` — Прочие сервисы

### Конфигурация эндпоинтов

```json
{
  "endpoints": [
    {
      "id": "main",
      "name": "Main Site",
      "url": "https://example.com/",
      "method": "HEAD",
      "timeout": 5000,
      "expectedStatus": [200, 301, 302]
    }
  ]
}
```

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | string | Уникальный ID эндпоинта |
| `name` | string | Отображаемое имя |
| `url` | string | URL для проверки |
| `method` | string | `HEAD`, `GET` или `POST` |
| `timeout` | number | Таймаут в мс (опционально) |
| `expectedStatus` | number[] | Ожидаемые HTTP статусы (опционально) |

### Разрешения

```json
{
  "permissions": {
    "http": ["example.com", "*.example.com"]
  }
}
```

Поддерживаются wildcards:
- `example.com` — только этот домен
- `*.example.com` — все поддомены
- `*` — любой домен (не рекомендуется)

## Примеры

### Простой сервис с одним эндпоинтом

```json
{
  "id": "github-checker",
  "name": "GitHub Checker",
  "version": "1.0.0",
  "type": "service-checker",
  "service": {
    "id": "github",
    "name": "GitHub",
    "icon": "🐙",
    "category": "other",
    "description": "Платформа для разработчиков",
    "endpoints": [
      {
        "id": "main",
        "name": "GitHub",
        "url": "https://github.com/",
        "method": "HEAD"
      }
    ]
  },
  "permissions": {
    "http": ["github.com"]
  }
}
```

### Сервис с несколькими эндпоинтами

```json
{
  "id": "spotify-checker",
  "name": "Spotify Checker",
  "version": "1.0.0",
  "type": "service-checker",
  "service": {
    "id": "spotify",
    "name": "Spotify",
    "icon": "🎵",
    "category": "media",
    "description": "Музыкальный стриминговый сервис",
    "endpoints": [
      {
        "id": "web",
        "name": "Web Player",
        "url": "https://open.spotify.com/",
        "method": "HEAD"
      },
      {
        "id": "api",
        "name": "API",
        "url": "https://api.spotify.com/",
        "method": "HEAD"
      },
      {
        "id": "cdn",
        "name": "CDN",
        "url": "https://i.scdn.co/",
        "method": "HEAD"
      }
    ]
  },
  "permissions": {
    "http": ["spotify.com", "*.spotify.com", "scdn.co", "*.scdn.co"]
  }
}
```

## Как это работает

1. Isolate загружает `plugin.json` при старте
2. Сервис добавляется в список проверяемых
3. При проверке Isolate делает HTTP запросы к каждому эндпоинту
4. Результаты отображаются в UI с учётом всех эндпоинтов

## Отладка

Если плагин не загружается:

1. Проверьте валидность JSON:
   ```bash
   cat plugins/my-service-checker/plugin.json | jq .
   ```

2. Убедитесь что `id` уникален

3. Проверьте логи Isolate на ошибки загрузки

## См. также

- [plugins/youtube-checker](../youtube-checker/) — пример реального плагина
- [plugins/discord-checker](../discord-checker/) — пример с несколькими эндпоинтами
